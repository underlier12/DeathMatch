<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.deathmatch.genius.mapper.IndianSettingMapper">
	
	<!-- 카드 가져 오기 -->
	<select id ="cardList" resultType ="IndianCardDTO">
		select * from tbl_IndianCard order by rand();
	</select>
	
	
</mapper>
